<!DOCTYPE html>
html
	head
		meta(charset="UTF-8")
		title AWAP Project

		meta(name="viewport", content="width=device-width, initial-scale=1.0, minimum-scale=1.0, maximum-scale=1.0, user-scalable=no")

		//- === === === === ===
		//- A little SEO
		//- === === === === ===

		meta(property="og:title", content="AWAP Project")
		meta(property="og:description", contents="Let's make online accounts without passwords")
		meta(property="og:image", contents="http://awap.pw/images/poster.jpg")

	body
		#app